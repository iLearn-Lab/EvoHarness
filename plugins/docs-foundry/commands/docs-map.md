---
description: Map the current documentation surface and identify the next repair target
argument-hint: Doc area or audience
allowed-tools: workspace_status,list_registry,mcp_registry_detail,tool_help,skill,mcp_call_tool,mcp_read_resource,read_file,grep,glob,run_subagent
---

# Docs Map

Focus: $ARGUMENTS

1. Load `doc-map` first.
2. Use `docs-foundry:docs-gap` to catalog docs before broad reading.
3. If useful, delegate one bounded pass to `doc-cartographer`.
4. End with the next doc area worth editing.

