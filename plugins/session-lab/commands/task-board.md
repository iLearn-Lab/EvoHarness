---
description: Review background task state and choose the next operational move
argument-hint: Task or queue focus
allowed-tools: workspace_status,list_registry,mcp_registry_detail,tool_help,skill,mcp_call_tool,mcp_read_resource,read_file,grep,glob,run_subagent
---

# Task Board

Focus: $ARGUMENTS

1. Load `task-ops` first.
2. Use `session-lab:session-lab` to inspect tasks and related session evidence.
3. If useful, delegate one bounded pass to `task-conductor`.
4. End with the next operational move.
