---
description: Investigate recent session behavior and extract the next workflow improvement target
argument-hint: Session symptom or area
allowed-tools: workspace_status,list_registry,mcp_registry_detail,tool_help,skill,mcp_call_tool,mcp_read_resource,read_file,grep,glob,run_subagent
---

# Session Forensics

Focus: $ARGUMENTS

1. Load `session-forensics` first.
2. Use `session-lab:session-lab` to inspect recent session evidence.
3. If useful, delegate one bounded pass to `session-detective`.
4. End with the next workflow improvement target.

