---
name: web-research
description: Search the public web first, then fetch only the best pages and summarize them with links.
---

# Web Research

- use `web_search` or the `web-research` MCP server first
- fetch only the most relevant pages instead of opening everything
- end with links, tradeoffs, and the best next step
- do not pretend you searched the web if you did not

