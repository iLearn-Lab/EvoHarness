---
description: Review and improve docs against the live harness surface
argument-hint: Doc or onboarding focus
allowed-tools: workspace_status,list_registry,mcp_registry_detail,tool_help,skill,mcp_call_tool,mcp_read_resource,read_file,grep,glob,run_subagent
---

# Docs Refresh

Focus: $ARGUMENTS

1. Load `docs-experience` first.
2. Inspect live commands, agents, plugins, and MCP before editing docs.
3. If useful, delegate one bounded pass to `docs-curator`.
4. End with the doc change that most reduces user confusion.

