---
description: Plan or review a local MCP service for this workspace
argument-hint: MCP capability gap
allowed-tools: workspace_status,list_registry,mcp_registry_detail,tool_help,skill,read_file,grep,glob,read_json,run_subagent
---

# MCP Blueprint

Gap: $ARGUMENTS

1. Load `mcp-service-design` first.
2. Check whether the gap should be a tool, resource, prompt, or all three.
3. If the interface is subtle, delegate one bounded pass to `mcp-designer`.
4. Finish with a bootable server shape and the registry metadata it needs.

