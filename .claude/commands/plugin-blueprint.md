---
description: Design or review a plugin bundle so it adds real harness value instead of just more files
argument-hint: Plugin concept or gap
allowed-tools: workspace_status,list_registry,mcp_registry_detail,tool_help,skill,read_file,grep,glob,read_json,run_subagent
---

# Plugin Blueprint

Target: $ARGUMENTS

1. Load `plugin-authoring` first.
2. Inspect nearby plugins, commands, skills, agents, and MCP assets.
3. If useful, delegate one bounded review to `plugin-architect`.
4. End with the leanest plugin bundle worth adding now.

